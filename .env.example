# Copy this file to `.env.local` and fill in values below.
# Do NOT commit any non-`.example` env files.

# Public API key for Tambo client integration (safe to expose in the browser; do not put secrets here)
NEXT_PUBLIC_TAMBO_API_KEY=YOUR_PUBLIC_TAMBO_API_KEY_HERE
